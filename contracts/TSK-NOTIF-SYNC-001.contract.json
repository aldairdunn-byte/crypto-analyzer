{
  "contract_version": "4.0",
  "status": "PASSED",
  "language": "typescript",
  "task_id": "TSK-NOTIFSYNC-001",
  "epic_id": "EPC-NOTIFSYNC",
  "goal": "Synchronize in-app notifications with authenticated user account across all devices using Supabase bot_trades as Single Source of Truth",
  "scope": {
    "target_files": [
      "frontend/src/contexts/BotEngineContext.tsx"
    ],
    "forbidden_files": [
      "frontend/src/App.tsx",
      "supabase/**/*",
      "app.py",
      "*.py"
    ],
    "authorized_global_files": []
  },
  "depends_on": [],
  "contracts": {
    "types_file": "frontend/src/lib/notifications.ts",
    "function_signatures": [
      "export const BotEngineProvider: React.FC<{ children: React.ReactNode }>"
    ]
  },
  "env": {
    "NODE_ENV": "test"
  },
  "execution_steps": [
    "Reconcile user-scoped notifications on auth state change in BotEngineContext",
    "Hydrate account profit and execution notifications directly from cloud bot_trades for cross-device logins",
    "Reset notifications state when switching accounts or returning to guest mode",
    "Run automated test suite, linter, and typecheck",
    "Deploy verified build to Vercel production"
  ],
  "commands": {
    "test": "node --test frontend/src/__tests__/account_notifications_sync.test.js",
    "lint": "npx oxlint frontend/src/contexts/BotEngineContext.tsx",
    "typecheck": "npm run build --prefix frontend"
  },
  "acceptance_criteria": [
    "assert.ok(contextContent.includes('syncNotificationsForUser'), 'BotEngineContext implements account notifications synchronization')",
    "assert.ok(contextContent.includes('createProfitNotification'), 'BotEngineContext creates notifications from cloud trades')"
  ],
  "definition_of_done": [
    "commands.test returns exit code 0",
    "commands.lint returns exit code 0",
    "commands.typecheck returns exit code 0",
    "Notifications synchronize across devices for authenticated user accounts",
    "Logged-out or guest mode does not leak authenticated user notifications",
    "git diff --name-only shows only target_files"
  ],
  "rollback": {
    "command": "git checkout HEAD -- frontend/src/contexts/BotEngineContext.tsx"
  }
}
